GCP_PROJECT_ID=your GCP project ID
GCP_REGION=the GCP region you will be using (e.g. `us-central1`)
GCP_BUCKET_NAME=the name of a GCS bucket you created!
PIPELINE_ROOT_PATH=gs://YOUR-BUCKET/pipeline_root
DEFAULT_INPUT_COLUMN=
DEFAULT_TARGET_COLUMN=
LOCAL_TMP_DIR=./tmp
ENDPOINT_ID=
PROJECT_NUMBER=
# Don't forget to add `.env` to your `.gitignore` files